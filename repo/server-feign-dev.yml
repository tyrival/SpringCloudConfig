eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/

feign:
  hystrix:
    enabled: true

logging:
  level:
    root: debug
  file: datacenter-log/feign-log:log

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: -1
      max-request-size: -1